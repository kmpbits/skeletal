android.useAndroidX=true
kotlin.native.ignoreDisabledTargets=true

# ── Maven Central coordinates ────────────────────────────────────────────────
GROUP=io.github.kmpbits
VERSION_NAME=0.1.0

# Publishing credentials (Central Portal user token + GPG signing key) are
# read from ~/.gradle/gradle.properties or ORG_GRADLE_PROJECT_* env vars.
# NEVER put mavenCentralUsername / mavenCentralPassword / signingInMemoryKey*
# in this file — it is committed to git.
